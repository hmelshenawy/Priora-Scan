// Type augmentations for jest-dom custom matchers (e.g. `toBeInTheDocument`).
// This file is referenced from tsconfig.types so it applies to all tests
// without each file needing its own `/// <reference>` directive.

import '@testing-library/jest-dom';
